numpy>=1.20.0
matplotlib>=3.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pyserial>=3.5
python-multipart>=0.0.9
# fastapi.testclient -> starlette.testclient needs an HTTP client that starlette
# does not pull in itself.  Without it tests/test_webapp_api.py fails to import
# with "requires the httpx2 package", which is easy to misread as a code bug.
httpx2>=2.0.0
