# Vivado generates these release scripts with intentional trailing whitespace.
# Preserve their bytes because SHA256SUMS verifies the exact programmed bundle.
/artifacts/**/*.tcl whitespace=-trailing-space,-blank-at-eof
