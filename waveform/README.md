# Waveform Samples

`waveform/master/` and `waveform/slave/` contain sample vectors captured or
prepared for the historical master and slave synchronization flows. They are
reference input data for playback/debug and are not selected by the hardware
role parameter or required to build either bitstream.

The common vectors under `waveform/` remain the default playback samples.
